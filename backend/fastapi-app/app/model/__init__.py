from app.model.call import Call
from app.model.report import Report
from app.model.news import News
from app.model.native_expression import NativeExpression

# 여기에 있는 모든 모델이 Alembic에서 인식됨
# Base.metadata에 등록된 테이블 자동 감지 가능
